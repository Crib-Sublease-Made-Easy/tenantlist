import styled from "styled-components";
import { LIGHTGREY, MEDIUMGREY, MEDIUMROUNDED, OPENSANS } from "../../sharedUtils";

export const SingupTitle = styled.p`
    font-size: 2rem;
    font-weight: 700;
    font-family: ${OPENSANS};
    margin-bottom: 2px;
`

export const SingupText = styled.p`
    font-size: 1rem;
    font-family: ${OPENSANS};
`

export const SingupSubheading = styled.p`
    font-size: 1.3rem;
    font-weight: 600;
    font-family: ${OPENSANS};
    margin-bottom: 2px;
`